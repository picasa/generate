% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/system_gis.R
\name{shift_lines}
\alias{shift_lines}
\title{Select ridge lines and compute vertical shift}
\usage{
shift_lines(data, z = "z", n_lines = 200, n_drop = 0, shift = 15, k = 0)
}
\arguments{
\item{data}{dataframe with x, y, \{z\} columns}

\item{z}{base column name for elevation (default "z"). Output column: \{z\}s}

\item{n_lines}{number of lines to select (0 = all)}

\item{n_drop}{number of lines to drop from the far end}

\item{shift}{vertical shift per rank (m)}

\item{k}{non-linear perspective coefficient (0 = linear)}
}
\value{
input dataframe with additional columns:
\itemize{
\item \code{y_rank}: integer rank of each line from foreground (1) to background
\item \code{y_dist}: normalized line position in 0:1
\item \code{dz}: vertical shift applied to this line (m)
\item \code{xn}: x coordinate relative to the left edge of each line (m)
\item \code{x_rank}: integer rank of each point within its line
\item \verb{\\\{z\\\}s}: shifted elevation (\{z\} + dz)
}
}
\description{
Steps 1-2 of the ridge pipeline: samples n_lines evenly from the y-axis,
assigns ranks and normalized distances, computes a perspective shift dz,
and adds the shifted column \{z\}s = \{z\} + dz.
}
